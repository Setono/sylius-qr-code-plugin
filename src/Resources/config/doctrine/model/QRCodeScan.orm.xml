<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:gedmo="http://gediminasm.org/schemas/orm/doctrine-extensions-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <mapped-superclass name="Setono\SyliusQRCodePlugin\Model\QRCodeScan" table="setono_sylius_qr_code__qr_code_scan">
        <!--
            Composite index on (qr_code_id, scanned_at) covers every per-QR query in
            QRCodeScanRepository:
              - countForQrCode / countForQrCodeSince (leading column match)
              - countDailyBuckets (range on scanned_at inside a qr_code_id group)
              - findRecentForQrCode (range + ORDER BY scanned_at DESC LIMIT, served from the
                already-ordered index so no filesort)
            This is the hot read path; scans are written once and read many times (grid
            scansCount + stats page + redirect-latency-free tracking).
        -->
        <indexes>
            <index name="setono_sylius_qr_code_scan_qr_code_scanned_at_idx" columns="qr_code_id,scanned_at"/>
        </indexes>

        <id name="id" type="integer" column="id">
            <generator strategy="AUTO"/>
        </id>

        <field name="scannedAt" type="datetime_immutable" column="scanned_at">
            <gedmo:timestampable on="create"/>
        </field>
        <!-- 45 = maximum length of an IPv6 address (e.g. an IPv4-mapped IPv6 address like ::ffff:255.255.255.255). -->
        <field name="ipAddress" type="string" length="45" column="ip_address"/>
        <field name="userAgent" type="string" length="512" column="user_agent"/>

        <many-to-one field="qrCode" target-entity="Setono\SyliusQRCodePlugin\Model\QRCode" inversed-by="scans">
            <join-column name="qr_code_id" referenced-column-name="id" nullable="false" on-delete="CASCADE"/>
        </many-to-one>
    </mapped-superclass>

</doctrine-mapping>
