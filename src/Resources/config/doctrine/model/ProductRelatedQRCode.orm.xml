<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <mapped-superclass name="Setono\SyliusQRCodePlugin\Model\ProductRelatedQRCode">
        <many-to-one field="product" target-entity="Sylius\Component\Product\Model\ProductInterface">
            <!-- product_id is nullable at the DB level because STI rows of other types do not
                 have a product; the application-layer NotNull validator enforces non-null for
                 product QR codes. -->
            <join-column name="product_id" referenced-column-name="id" nullable="true" on-delete="CASCADE"/>
        </many-to-one>
    </mapped-superclass>

</doctrine-mapping>
